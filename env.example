# Nullism Website Environment Variables
# Set the initial global orbit counter (optional)
# If not set, the counter will start from 0 or read from the .orbit_counter file

GLOBAL_ORBIT_COUNTER=0

# Server port (optional, defaults to 3000)
PORT=3000 